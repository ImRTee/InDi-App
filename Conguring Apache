# For httpd 2.2, you can go straight to /etc/httpd/conf/httpd.conf 
#For httpd 2.4, create a conf file in conf.d
vi /etc/httpd/conf.d/[project-name].conf

# For httpd  2.4, in httpd.conf make sure that there is the line 
IncludeOptional conf.d/*.conf #To include all conf file in conf.d folder
# For httpd 2.2, go to the bottom where you will see the virtual host section 
# Remove the comment symbol '#'
# Edit it as the following
<VirtualHost *>
    ServerAdmin admin
    DocumentRoot var/www/html/document
    ServerName example.com

    WSGIDaemonProcess xproject python-path=/var/www/[project-name]:/var/www/[project-name]/venv/lib/python2.7/site-packages threads=5
    WSGIScriptAlias / /var/www/[project-name]/flask.wsgi
    WSGIProcessGroup xproject

    <Directory /var/www/[project-name]>
        Order deny,allow #For httpd 2.2
        Allow from all # For httpd 2.2
        Require all granted #For httpd 2.4
    </Directory>

    ErrorLog /var/www/[project-name]/error.log
    CustomLog /var/www/[project-name]/custom.log combined
</VirtualHost>